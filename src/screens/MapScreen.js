import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const MapScreen = () => {
  return (
    <View>
      <Text>Here is the map stuf...</Text>
    </View>
  )
}

export default MapScreen;

const styles = StyleSheet.create({})